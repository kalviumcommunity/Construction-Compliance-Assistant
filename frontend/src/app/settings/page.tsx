import SettingsPage from "@/components/pages/SettingsPage";

export default function Page() {

  const metadata = {
    title: "Settings | SiteCode AI",
    description: "Settings for Construction Compliance",
  }

  return <SettingsPage />;
}
