import SearchPage from "@/components/pages/SearchPage";

export default function Page() {

  const metadata = {
    title: "Search | SiteCode AI",
    description: "Search for Construction Compliance",
  }

  return <SearchPage />;
}
